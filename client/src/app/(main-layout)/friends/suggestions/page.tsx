import { ReactNode } from "react";

export default function FriendSuggestion({ children }: { children: ReactNode }) {
    return (
        <div className=" mx-auto">
            <h1 className="text-2xl font-bold">Friends suggestion</h1>
        </div>
    );
}
