import { ReactNode } from "react";

export default function FriendRequests({ children }: { children: ReactNode }) {
    return (
        <div className="mx-auto h-[120vh]">
            <h1 className="text-2xl font-bold">home friends</h1>
        </div>
    );
}
