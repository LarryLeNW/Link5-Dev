import { ReactNode } from "react";

export default function FriendBirthday({ children }: { children: ReactNode }) {
    return (
        <div className="mx-auto h-[120vh]">
            <h1 className="text-2xl font-bold">birthday friends</h1>
        </div>
    );
}
